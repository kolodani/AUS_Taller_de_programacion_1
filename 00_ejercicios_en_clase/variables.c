#include <stdio.h>

int main()
{
    const int cant_prod = 1200; // variable int
    double x;
    //cant_prod = -1200;  //asignacion  (l-value)

    //cant_prod = cant_prod + 1;       // (l-value, r-value)

    printf("Veamos... %d\n", cant_prod);

    return 0;
}